package com.iflas;

/**
 * Interface for Regional Cpu and Ram specifications.
 * It implements toString method for specify, CPU core number.
 * @author Furkan Kalabalık
 *
 */
public interface CpuRamSpec {
	/**
	 * Generates cpu core number.
	 * @return Cpu core number in string
	 */
	public String toString();
}
